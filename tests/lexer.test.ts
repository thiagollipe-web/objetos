import{describe,it,expect}from"vitest";import{Lexer,normalizeWord}from"../src/lang/lexer";describe("Bit lexer",()=>{it("normaliza acentos",()=>expect(normalizeWord("Então")).toBe("entao"));it("lê números",()=>expect(new Lexer("123 4.5").tokenize().filter(t=>t.kind==="NUMBER")).toHaveLength(2));it("lê operadores",()=>expect(new Lexer("a += 2 && b").tokenize().map(t=>t.kind)).toEqual(expect.arrayContaining(["PLUS_EQ","AND"])))})